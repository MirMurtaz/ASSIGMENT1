#include <stdio.h>

int main() {
    int A, B, sum;
    printf("Enter the first integer (A): ");
    scanf("%d", &A);
    printf("Enter the second integer (B): ");
    scanf("%d", &B);  
    sum = A + B;
    printf("The sum of %d and %d is: %d\n", A, B, sum);

    return 0;  
}
